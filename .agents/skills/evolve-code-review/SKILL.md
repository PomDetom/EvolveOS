# evolve-code-review

Review is semantic judgment, not a workflow state. Read the request, relevant AGENTS.md, changed paths, related Notes, and current verification evidence. Write `.agents/tasks/<year>/<task-id>/review.md` only when a recovery task exists, bind it to `Subject head`, record the real result and findings, and never claim approval without inspecting the diff.
