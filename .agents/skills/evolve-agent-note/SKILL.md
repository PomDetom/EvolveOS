# evolve-agent-note

Create a Note only for a cross-task decision: shared contracts, workflow/build/release policy, Tauri permissions/persistence, testing strategy, security, or a meaningful trade-off. Use `proposed` for an undecided direction and move the same Note to `implemented` when delivered. Do not duplicate implementation logs or create a second Note for an existing decision.
