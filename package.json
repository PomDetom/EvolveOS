{
  "name": "evolveos",
  "private": true,
  "version": "0.1.3",
  "type": "module",
  "description": "EvolveOS — 多应用综合管理系统：统一应用壳承载多桌面应用，共享设计系统为基础设施。",
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:e2e": "playwright test",
    "test:visual": "playwright test tests/e2e/visual-regression.spec.js",
    "check:boundary": "node scripts/check-boundary.js",
    "set-version": "node scripts/set-version.js",
    "release": "node scripts/release.js",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "@tauri-apps/cli": "^2.11.4",
    "jsdom": "^26.0.0",
    "vite": "^7.0.0",
    "vitest": "^3.0.0"
  }
}
